% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_client.R
\name{training_set_config}
\alias{training_set_config}
\alias{training_set_config_safe}
\title{Update training-pipeline configuration}
\usage{
training_set_config(
  classes = NULL,
  system_prompt = NULL,
  user_prompt_template = NULL,
  llm = NULL,
  ml_service_url = NULL
)

training_set_config_safe(
  classes = NULL,
  system_prompt = NULL,
  user_prompt_template = NULL,
  llm = NULL,
  ml_service_url = NULL
)
}
\arguments{
\item{classes}{Список классов \code{list(list(name=..., description=...), ...)}.
Передавайте только если хотите перезаписать список целиком.}

\item{system_prompt}{Строка — system-prompt для LLM-разметки.}

\item{user_prompt_template}{Шаблон user-prompt с плейсхолдерами:
\code{\{title\}}, \code{\{abstract\}}, \code{\{classes_list\}}, \code{\{classes\}}.}

\item{llm}{Именованный список LLM-настроек: \code{provider} (один из
\code{"openai"}, \code{"anthropic"}, \code{"grok"}), \code{model},
\code{base_url}, \code{api_key}, \code{temperature}, \code{max_tokens},
\code{concurrency}, \code{request_timeout_secs}. Пустой \code{api_key}
не затирает существующий — пропустите ключ совсем, чтобы оставить как есть.}

\item{ml_service_url}{URL ML-сервиса.}
}
\value{
Сохранённый конфиг (с замаскированным API-ключом).
\code{training_set_config_safe()} возвращает \code{list(ok, data, error, status)}.
}
\description{
Передаёт частичное обновление конфигурации в ML-сервис
(\code{POST /training/config}). Меняются только указанные поля.
}
\examples{
\dontrun{
training_set_config(
  llm = list(provider = "grok", model = "grok-3", api_key = "xai-..."),
  classes = list(
    list(name = "malware", description = "Malicious software"),
    list(name = "other",   description = "Anything else")
  )
)
}
}
\seealso{
\code{\link{training_get_config}}
}
