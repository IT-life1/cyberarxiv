% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_publications.R
\name{load_publications}
\alias{load_publications}
\title{Load publications from local DuckDB storage}
\usage{
load_publications(
  db_path = NULL,
  year = NULL,
  category = NULL,
  text = NULL,
  limit = NULL
)
}
\arguments{
\item{db_path}{Path to DuckDB file. If NULL, uses internal default resolution:
env \code{CYBERARXIV_DB_PATH} -> option \code{cyberarxiv.db_path} -> \code{data/cyberarxiv.duckdb}.}

\item{year}{Optional integer year to filter by \code{published_date} (e.g., 2025).}

\item{category}{Optional string. Keeps rows where \code{categories} contains this substring
(case-insensitive). Note: \code{categories} are stored as human-readable names joined by ", ".}

\item{text}{Optional string. Case-insensitive search in \code{title} OR \code{abstract}.}

\item{limit}{Optional integer to limit number of rows returned (applied after filtering).}
}
\value{
A data.frame (or tibble if \code{tibble} installed) with columns:
\describe{
\item{paper_id}{arXiv identifier (e.g. "2512.13613")}
\item{link}{arXiv URL}
\item{title}{Title}
\item{authors}{Authors as a comma-separated string}
\item{abstract}{Abstract/summary}
\item{categories}{Human-readable categories as a comma-separated string}
\item{published_date}{POSIXct timestamp (UTC) when first published}
\item{updated_date}{POSIXct timestamp (UTC) when last updated on arXiv}
\item{ingested_at}{Timestamp when record was inserted/updated in local DB}
}
}
\description{
Reads publications previously saved into the local DuckDB database (table \code{papers}).
By default returns all rows. Optional filters can limit the result by year, category,
or free-text search over title/abstract.
}
