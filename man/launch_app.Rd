% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/shiny_app.R
\name{launch_app}
\alias{launch_app}
\title{CyberArXiv Shiny GUI}
\usage{
launch_app(
  host = "0.0.0.0",
  port = 3838,
  ml_service_url = NULL,
  launch_browser = interactive()
)
}
\arguments{
\item{host}{Хост для Shiny-сервера (по умолчанию "0.0.0.0").}

\item{port}{Порт (по умолчанию 3838).}

\item{ml_service_url}{URL ML-сервиса (по умолчанию из env var
\code{ML_SERVICE_URL} или \code{http://localhost:5001}).}

\item{launch_browser}{Открывать ли браузер автоматически (TRUE для
локального запуска, FALSE для Docker/удалённого).}
}
\value{
Ничего не возвращает, блокирует выполнение пока Shiny работает.
}
\description{
Интерактивное веб-приложение (Shiny) для управления пайплайном CyberArXiv:
загрузка статей с arXiv, ML-классификация, поиск, фильтрация, просмотр
таблицы и графиков. Альтернатива дашборду для пользователей,
которые предпочитают GUI вместо командной строки.
}
\section{Функциональность}{

\itemize{
  \item Вкладка "Обзор": метрики + диаграммы.
  \item Вкладка "Загрузка (ETL)": скачивание с arXiv, прогресс, лог.
  \item Вкладка "Таблица": поиск, фильтры (год/тег/ml_tag), CSV-экспорт.
  \item Вкладка "ML-классификатор": статус сервиса, пакетная и ad-hoc
    классификация одной аннотации.
  \item Вкладка "Аналитика": топ-авторы, топ-слова, кросс-таблица
    keyword-тег vs ML-тег.
  \item Вкладка "Настройки": пути, переинициализация схемы.
}
}

\examples{
\dontrun{
  library(cyberarxiv)
  launch_app()
  launch_app(port = 8080)
  launch_app(launch_browser = FALSE)
}
}
