% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/collector_registry.R
\name{collect_all}
\alias{collect_all}
\title{Collect papers from all (or selected) sources}
\usage{
collect_all(
  sources = NULL,
  max_results = 100L,
  extra_dirs = NULL,
  param_overrides = list()
)
}
\arguments{
\item{sources}{Character vector of collector names to run.
\code{NULL} (default) runs every enabled collector.}

\item{max_results}{Maximum papers to fetch \strong{per source}.}

\item{extra_dirs}{Additional directories to scan for collector YAML files.}

\item{param_overrides}{Named list of per-collector parameter overrides.
E.g. \code{list(arxiv = list(params = list(search_query = "cat:cs.CR")))}
merges into the spec before the collector runs.}
}
\value{
data.frame with columns:
\code{id, source, link, title, authors, abstract, categories,
  published_date, updated_date}.
}
\description{
Runs every enabled collector and combines results into a single data.frame.
Each record carries a \code{source} column with the collector name.
}
