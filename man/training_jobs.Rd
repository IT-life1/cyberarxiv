% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_client.R
\name{training_jobs}
\alias{training_start_collect}
\alias{training_start_label}
\alias{training_start_export_excel}
\alias{training_start_train}
\alias{training_get_job}
\alias{training_list_jobs}
\alias{training_cancel_job}
\title{Async training-pipeline jobs}
\usage{
training_start_collect(target, query = NULL, page_size = 200,
                       ml_service_url = NULL)

training_start_label(raw_path, max_rows = 0L, ml_service_url = NULL)

training_start_export_excel(labeled_path, max_rows = 0L,
                            ml_service_url = NULL)

training_start_train(excel_path, model_name_out = "custom_model",
                     base_model = "distilbert-base-uncased",
                     epochs = 8L, batch_size = 16L, lr = 2e-5,
                     max_length = 256L,
                     test_size = 0.1, val_size = 0.1,
                     mlflow_tracking_uri = NULL,
                     ml_service_url = NULL)

training_get_job(job_id, ml_service_url = NULL)

training_list_jobs(type = NULL, limit = 50L, ml_service_url = NULL)

training_cancel_job(job_id, ml_service_url = NULL)
}
\arguments{
\item{target}{Цель по строкам в собираемом arXiv-датасете (10..200000).}

\item{query}{arXiv \code{search_query}; NULL = дефолт по кибербезопасности.}

\item{page_size}{Размер страницы запросов к arXiv (50-2000, чаще 200).}

\item{raw_path}{Имя файла из \code{training_data/raw/}, например
\code{"arxiv_20260430_224942_1000.parquet"}.}

\item{max_rows}{Сколько строк размечать (0 = все). Полезно для пробного прогона.}

\item{labeled_path}{Имя файла из \code{training_data/labeled/}.}

\item{excel_path}{Имя файла из \code{training_data/excel/}.}

\item{model_name_out}{Имя итогового \code{.pt} (без расширения).
Файл попадёт в \code{MODELS_DIR}.}

\item{base_model}{HuggingFace-чекпойнт для энкодера (по умолчанию
\code{distilbert-base-uncased}).}

\item{epochs, batch_size, lr, max_length}{Гиперпараметры обучения.}

\item{test_size, val_size}{Доли валидации и теста (по 0.1 = 20\% всего).
Понизьте на маленьких датасетах, иначе train_test_split дропнет редкие классы.}

\item{mlflow_tracking_uri}{URL MLflow tracking сервера. Пусто/NULL =
прочитать из env \code{MLFLOW_TRACKING_URI}.}

\item{job_id}{UUID джоба, возвращённый одной из \code{training_start_*}.}

\item{type}{Фильтр по типу джоба: \code{collect}, \code{label},
\code{export_excel}, \code{train}.}

\item{limit}{Сколько последних джобов вернуть.}

\item{ml_service_url}{URL ML-сервиса.}
}
\value{
\code{training_start_*} возвращают строку \code{job_id}.

\code{training_get_job()} — список с полями \code{id, type, status, progress,
log, params, result, error, created_at, updated_at}. Возвращает NULL, если
джоб не найден или сервис недоступен.

\code{training_list_jobs()} — список таких же объектов, отсортированный по
\code{updated_at}.

\code{training_cancel_job()} — обновлённый объект (статус \code{"cancelled"}).
}
\description{
Все четыре шага обучения (\code{collect}, \code{label}, \code{export_excel},
\code{train}) запускаются как фоновые job-ы в ML-сервисе. Состояние
сохраняется в \code{training_data/jobs/<id>.json} и переживает рестарт.
}
\examples{
\dontrun{
jid <- training_start_collect(target = 1000L)
repeat {
  s <- training_get_job(jid)
  cat(s$status, sprintf("\%.0f\%\%", (s$progress \%||\% 0)*100), "\n")
  if (s$status \%in\% c("completed","failed","cancelled")) break
  Sys.sleep(5)
}
}
}
\seealso{
\code{\link{training_get_config}}, \code{\link{training_list_files}}
}
