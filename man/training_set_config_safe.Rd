% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_client.R
\name{training_set_config_safe}
\alias{training_set_config_safe}
\title{Like training_set_config but returns the full call result (ok/data/error).
Pass empty payload (everything NULL) and you'll get back the current config.}
\usage{
training_set_config_safe(
  classes = NULL,
  system_prompt = NULL,
  user_prompt_template = NULL,
  llm = NULL,
  ml_service_url = NULL
)
}
\arguments{
\item{classes}{List of class definitions for the LLM labeler, e.g.
\code{list(list(name="Malware", description="..."), ...)}. \code{NULL} keeps the current value.}

\item{system_prompt}{String. System prompt for the LLM labeler. \code{NULL} keeps the current value.}

\item{user_prompt_template}{String. User-message template (uses \code{\{title\}}/\code{\{abstract\}}
placeholders). \code{NULL} keeps the current value.}

\item{llm}{Named list with LLM credentials/config (provider, model, api_key, base_url, ...).
\code{NULL} keeps the current value.}

\item{ml_service_url}{Service URL. Defaults to env var \code{ML_SERVICE_URL}.}
}
\description{
Like training_set_config but returns the full call result (ok/data/error).
Pass empty payload (everything NULL) and you'll get back the current config.
}
