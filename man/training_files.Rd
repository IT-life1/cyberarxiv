% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training_client.R
\name{training_files}
\alias{training_list_files}
\alias{training_download_file}
\alias{training_reload_models}
\title{Browse and reload training-pipeline artifacts}
\usage{
training_list_files(category = c("raw", "labeled", "excel"),
                    ml_service_url = NULL)

training_download_file(category, filename, dest_path, ml_service_url = NULL)

training_reload_models(ml_service_url = NULL)
}
\arguments{
\item{category}{Один из \code{"raw"} (parquet до разметки),
\code{"labeled"} (parquet после LLM), \code{"excel"} (xlsx).}

\item{filename}{Имя файла из соответствующей папки. Lock-файлы редакторов
(\code{.~lock.*}, \code{~$*}) на стороне сервера отфильтрованы.}

\item{dest_path}{Куда сохранить локально.}

\item{ml_service_url}{URL ML-сервиса.}
}
\value{
\code{training_list_files()} — список объектов \code{list(name, size, mtime)}.

\code{training_download_file()} — путь к скачанному файлу (invisibly).

\code{training_reload_models()} — список с полем \code{models}, где имена всех
\code{.pt} в \code{MODELS_DIR}.
}
\description{
Утилиты для работы с артефактами training-пайплайна и hot-reload-а
обученных моделей в инференс-сервис без перезапуска контейнера.
}
\examples{
\dontrun{
# После training_start_train() и завершения джоба:
training_reload_models()$models
# [1] "best_model" "my_grok_model"

# Скачать готовый Excel для проверки на рабочей машине:
files <- training_list_files("excel")
training_download_file("excel", files[[1]]$name, "/tmp/dataset.xlsx")
}
}
