server {
    listen 8000;
    server_name _;

    root /var/www/html;
    index dashboard.html index.html;

    access_log /var/log/nginx/cyberarxiv.access.log;
    error_log /var/log/nginx/cyberarxiv.error.log;

    location / {
        try_files $uri $uri/ /dashboard.html =404;
    }
}
